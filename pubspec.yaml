name: progress_file_upload
description: "A new Flutter project."

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.0
  flutter_screenutil: ^5.9.3
  image_picker: ^1.1.2
  permission_handler: ^12.0.1
  cloud_firestore: ^6.0.1
  firebase_core: ^4.1.0







dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
